@page
@model WebApp1.Pages.Friends.IndexModel

@{
    ViewData["Title"] = "Friends";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Friends</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Friend[0].User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Friend[0].CurrentFriend)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Friend[0].User.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Friend[0].User.Email)
            </th>


            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Friend) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentFriend.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentFriend.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentFriend.Email)
            </td>

            <td>
@*                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
*@
                <a asp-page="../Messages/Index" asp-route-id="@item.Id">Messages</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
